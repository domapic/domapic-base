language: node_js

node_js:
  - "8"

cache:
  directories:
    - "node_modules"
    - "$HOME/.sonar/cache"

addons:
  sonarcloud:
    organization: "domapic"
    token:
      secure: "$SONAR_TOKEN"
    branches:
      - ci-tools-integration
      - master

script:
  - npm test
  - sonar-scanner
