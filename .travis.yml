language: node_js

node_js:
  - "8"

cache:
  directories:
    - "node_modules"
    - "$HOME/.sonar/cache"

addons:
  sonarcloud:
    organization: "domapic"
    token:
      secure: "$SONAR_TOKEN"
    branches:
      - master
      - integration-tests

script:
  - npm test
  - npm run coveralls
  - sonar-scanner
