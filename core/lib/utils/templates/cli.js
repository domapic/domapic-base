'use strict'

// TODO, review unused templates

module.exports = {
  startingService: 'Starting "{{name}}" service in background',
  startServiceHelp: 'Run "domapic start {{name}}" to start the service again',

  stoppingService: 'Stopping "{{name}}" service brackground process',
  stopServiceHelp: 'Run "domapic stop {{name}}" to stop the service',

  displayingLogs: 'Displaying logs for "{{name}}" service',
  displayLogsHelp: 'Run "domapic logs {{name}}" to display real-time logs'
}
